import { Team, Player } from "../types/types";

export const splitIntoTeams = (players: Player[], teamCount: number): Team[] => {
  // チーム分けロジックをここに実装
  // プレイヤーをチーム数に応じて分割するアルゴリズムを実装する必要があります。
  // 例えば、プレイヤーの参加回数を考慮してチームを分割することができます。
  // また、余ったプレイヤーを不参加にできる機能も実装する必要があります。
  return [];
};